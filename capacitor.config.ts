import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'firebase-analytics',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
